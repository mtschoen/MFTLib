{
  "name": "mftlib-ci-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "CI-only tooling for MFTLib (not shipped). Pins the @schoen/aislop quality gate to a local workspace checkout so CI and local scans use the same built toolchain.",
  "devDependencies": {
    "@schoen/aislop": "file:../../../aislop"
  }
}
