#pragma once

// The MFT parser now owns ntfs_io helpers inside the mft component
// translation unit. Keep this header only as a compatibility placeholder
// for old include paths; do not add component-private declarations here.
